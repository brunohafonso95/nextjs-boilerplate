import { Meta } from '@storybook/react';

import {{pascalCase name}} from '.';

export default {
  title: '{{pascalCase name}} Component',
  component: {{pascalCase name}},
} as Meta;

export const Basic = (args) => <{{pascalCase name}} {...args} />;
