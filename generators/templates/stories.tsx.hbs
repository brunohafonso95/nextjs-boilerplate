import { Meta, Story } from '@storybook/react';

import {{pascalCase name}} from '.';
import { {{pascalCase name}}Props } from './interfaces';

export default {
  title: '{{pascalCase name}} Component',
  component: {{pascalCase name}},
} as Meta;

export const Basic: Story<{{pascalCase name}}Props> = (args) => <{{pascalCase name}} {...args} />;
